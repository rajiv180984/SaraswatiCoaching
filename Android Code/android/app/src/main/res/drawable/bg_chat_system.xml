<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Grey bubble — top-left corner is squared to point toward the left -->
    <corners
        android:topLeftRadius="4dp"
        android:topRightRadius="@dimen/radius_component_large"
        android:bottomLeftRadius="@dimen/radius_component_large"
        android:bottomRightRadius="@dimen/radius_component_large" />
    <solid android:color="@color/surface_moderate" />
</shape>
