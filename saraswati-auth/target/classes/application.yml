server:
  port: 8443
  ssl:
    enabled: true
    enabled-protocols: TLSv1.3
    key-store: classpath:keystore.p12
    key-store-password: saraswati@123
    key-store-type: PKCS12
    key-alias: saraswati

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/saraswati_auth?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Zerox@180984
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: YOUR_EMAIL@gmail.com
    password: YOUR_GMAIL_APP_PASSWORD
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  jwt:
    # 64-byte hex secret — replace with your own (run: openssl rand -hex 64)
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration: 86400000        # 24 hours (ms)
    refresh-expiration: 604800000  # 7 days (ms)

  frontend-url: https://localhost:3000
  password-reset-token-expiry: 15  # minutes

logging:
  level:
    com.saraswati: DEBUG
    org.springframework.security: INFO
